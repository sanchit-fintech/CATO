[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "cato-agent"
version = "0.9.1"
description = "A local-first CLI personal-agent prototype"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1",
    "google-genai>=1.0,<3",
    "python-dotenv>=1.0,<2",
    "uvicorn>=0.34,<1",
]

[project.optional-dependencies]
dev = [
    "httpx>=0.27,<1",
    "pytest>=8,<10",
    "ruff>=0.11,<1",
]
voice = [
    "faster-whisper>=1.1,<2",
    "numpy>=2,<3",
    "sounddevice>=0.5,<1",
]

[project.scripts]
cato = "core.cato:main"

[tool.setuptools.packages.find]
include = ["core*", "tools*", "memory*", "cato_platform*", "voice*", "client*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
target-version = "py311"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
